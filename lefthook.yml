pre-commit:
  commands:
    miniapp-build:
      glob: "miniapp-src/**/*"
      run: npm run miniapp:build && git add public/miniapp

pre-push:
  parallel: true
  commands:
    db-check:
      run: npm run db:check
    lint:
      run: npm run lint
